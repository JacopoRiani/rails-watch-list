<div class="container">
   <div class="row mt-3">
      <div class="col-md-4">
        <div class="card">

          <div class="card-body">
        <h2><%= @list.name %></h2>
        <%= link_to "Back to lists", lists_path, class: "btn btn-primary mr-2"%>
        <%= link_to "Go to bookmarks", new_list_bookmark_path(@list), class: "btn btn-primary"%>
      </div>
      </div>
    </div>
    </div>
</div>
